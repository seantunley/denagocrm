// The @modal parallel slot renders nothing unless an intercepting route is
// active (e.g. a settings section opened from the sidebar flyout).
export default function ModalDefault() {
  return null;
}
